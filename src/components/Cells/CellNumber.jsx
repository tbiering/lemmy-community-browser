function CellNumber({ value }) {
  // Return two lines, the first being the name, the second being the title
  return (
    <div className="text-sm font-medium text-gray-900 text-center">{value}</div>
  );
}

export default CellNumber;
